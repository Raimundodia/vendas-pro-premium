const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0f0f1a',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100vw',  // Força largura total no navegador
    height: '100vh', // Força altura total no navegador
  },
});
